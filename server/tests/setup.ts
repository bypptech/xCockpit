// Jest setup file for x402 enhanced tests\n\n// Set test environment variables\nprocess.env.NODE_ENV = 'test';\nprocess.env.NETWORK = 'sepolia';\nprocess.env.ENHANCED_X402 = 'true';\nprocess.env.X402_HMAC_SECRET = 'test-secret-key-32-chars-long-12345';\nprocess.env.BASE_SEPOLIA_RPC = 'https://sepolia.base.org';\nprocess.env.BASE_MAINNET_RPC = 'https://mainnet.base.org';\nprocess.env.PAYMENT_RECIPIENT = '0x1c7d4b196cb0c7b01d743fbc6116a902379c7238';\nprocess.env.DEBUG_X402 = 'false';\nprocess.env.MOCK_BLOCKCHAIN = 'true';\n\n// Global test utilities\nglobal.console = {\n  ...console,\n  // Suppress console.log during tests unless DEBUG is set\n  log: process.env.DEBUG_TESTS === 'true' ? console.log : jest.fn(),\n  debug: process.env.DEBUG_TESTS === 'true' ? console.debug : jest.fn(),\n  info: console.info,\n  warn: console.warn,\n  error: console.error,\n};\n\n// Mock setTimeout for faster tests\njest.mock('setTimeout', () => (fn: Function, delay: number) => {\n  if (delay > 1000) {\n    // Speed up long delays in tests\n    return setTimeout(fn, Math.min(delay / 100, 100));\n  }\n  return setTimeout(fn, delay);\n});